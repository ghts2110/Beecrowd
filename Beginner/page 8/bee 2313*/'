#include <stdio.h>
#include <math.h>

int main(){
    int a, b, ci, max;
    scanf("%d %d %d", &a, &b, &c);
    max = max(a, b, c);
    if(){

    }else{
        printf("Invalido\n");
    }
    return 0;
}
